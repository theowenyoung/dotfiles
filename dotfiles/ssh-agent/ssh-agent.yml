# <https://stackoverflow.com/a/38980986>

actions:
  - action: file.link
    source: ssh-agent.service
    target: "{{user.home_dir}}/.config/systemd/user/ssh-agent.service"
  - action: command.run
    command: systemctl
    args:
      - --user 
      - enable 
      - ssh-agent
  - action: command.run
    command: systemctl
    args:
      - --user 
      - restart
      - ssh-agent