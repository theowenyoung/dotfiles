actions:
  - action: file.copy
    from: gost.json.tmpl
    to: "{{user.home_dir}}/.config/gost/gost.json"
    template: true
  - action: file.copy
    from: gost.service.tmpl
    to: "{{user.home_dir}}/.config/systemd/user/gost.service"
    template: true
  - action: command.run
    command: "{{user.home_dir}}/env/dotfiles/gost/files/install_gost_ssl.sh"
    args:
      - hello
        # command:"{{user.home_dir}}/env/dotfiles/gost/files/install_gost_ssl.sh"
