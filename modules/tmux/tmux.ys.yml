- use: fs.ensureSymlink
  args:
    - ${__dirname}/files/dot_tmux.conf
    - ${user.homeDir}/.tmux.conf
- use: fs.ensureSymlink
  args:
    - ${__dirname}/files/tmux_alias.zsh
    - ${user.homeDir}/.zsh/extra/tmux_alias.zsh