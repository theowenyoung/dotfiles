- use: fs.ensureSymlink
  args:
    - ${__dirname}/files/config.toml
    - ${user.homeDir}/.config/helix/config.toml
- use: fs.ensureSymlink
  args:
    - ${__dirname}/files/languages.toml
    - ${user.homeDir}/.config/helix/languages.toml